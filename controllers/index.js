// Xử lý Slide Banner

const listBanner = document.querySelector(".list-banner")
const banner = document.querySelectorAll(".banners")

// console.log(banner)
setInterval(() => {
    let width = banner[0].offsetWidth
    console.log.apply(width)
},4000)